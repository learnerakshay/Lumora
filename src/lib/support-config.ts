// Single source of truth for Lumora's support contact address. Referenced by
// the Contact page, the Report a Bug flow, and legal pages — change it here
// once the mailbox is live instead of editing every component that shows it.
export const SUPPORT_EMAIL = 'support@getlumora.in';
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
